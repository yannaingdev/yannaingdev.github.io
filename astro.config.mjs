// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
     site: "https://yannaing.github.io",
  // base is "" for user pages (NOT "/repo-name")
  base: "",
});
